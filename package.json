{
  "name": "nuvem",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "lint": "biome check .",
    "format": "biome check --write .",
    "check:docs": "markdownlint-cli2 '**/*.md' '#node_modules'",
    "check": "npm run lint && npm run check:docs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7",
    "@cloudflare/vitest-pool-workers": "0.10.10",
    "markdownlint-cli2": "^0.19.1",
    "typescript": "5.5.2",
    "vitest": "3.2.0",
    "wrangler": "4.50.0"
  }
}
